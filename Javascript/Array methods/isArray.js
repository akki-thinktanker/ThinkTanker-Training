const names = ['akki','vivek', 'vaibhavi']
const  str = 'hello'
const num = 18

const res1 = Array.isArray(names)
const res2= Array.isArray(str)
const res3= Array.isArray(num)

console.log(res1)
console.log(res2)
console.log(res3)