const num = [1,2,3,4,5,6,7]


// you can add, remove elements in array, it will change original array

// remove last 3 elem

const deleted = num.splice(2, 3,0,0,0) // we can add elements at that position provided

console.log(num)
console.log(deleted)